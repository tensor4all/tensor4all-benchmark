# Agent guidance

Read `README.md` before changing case definitions, timing boundaries, input generation, or environment settings.

## Maintained scope

The repository has exactly three benchmark cases: Fourier elementwise, Gaussian elementwise global versus patched, and Gaussian MPO-MPO contraction global versus patched. Do not add another case when an arm or metric in one of these cases answers the question.

Gaussian inputs use independent two-dimensional interpolative decomposition per term, deterministic balanced pairwise add-and-compress, final relative-L2/SVD tolerance `1e-6`, and fixed patch cap 128. Do not add TCI generation, whole-mixture interpolation, isotropic controls, independent `R` sweeps, or a runtime patch-cap knob without an explicit design change.

ACI residual tolerance is not an L2 tolerance. Keep its internal metric explicit and compare it with other arms only through the common achieved external error.

## Timing

Time only the benchmarked elementwise or contraction operation. Input construction, cache I/O, compression, conversion, patch preparation, output conversion, and accuracy evaluation stay outside timed regions.

Run release builds with one pinned CPU core and single-threaded Rayon and BLAS. Warm up any JIT-compiled external implementation before timing it.

## Results

Files under `result/<profile>/` are generated by `scripts/run_all.sh <profile>` and committed deliberately. Do not edit them manually. Probe runs belong under `/tmp`. One profile belongs to one physical machine and contains no hostname.

## Input cache

Every expensive deterministic input must use the shared ignored `.cache/inputs/` directory by default.

- Include every content-defining parameter, seed, upstream revision, and schema version in the key.
- Write atomically and treat existing entries as immutable.
- Validate loaded length, site dimensions, scalar type, and index compatibility.
- Record key, hit or miss, load time, and build time.
- Cache inputs only, never timed outputs.
- Input-generation and serialization tests must force a rebuild.

## Upstream

All tensor4all-rs crates are pinned to one revision in `Cargo.toml`. Move every pin together. A pin change requires fresh measurements.

## Prose

Do not use em dashes or en dashes in documentation, comments, or generated reports.
